
import { DosageConfig } from './types';

export const PANADOL_CONFIG: DosageConfig = {
  concentration: 24, // 24 mg / ml
  dosagePerKg: 15,   // 15 mg / kg
};

export const MAX_WEIGHT = 30;
export const MIN_WEIGHT = 3;
